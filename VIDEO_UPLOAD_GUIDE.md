# 🎥 Видео загрузка для Samples - Готово!

## ✅ Что добавлено:

### 1. **Компонент VideoUpload** (`src/components/ui/VideoUpload.tsx`)
- **Два режима загрузки**:
  - 📁 **Upload File**: загрузка видео файла на сервер
  - 🔗 **Enter URL**: ввод прямой ссылки на видео
- **Drag & Drop** поддержка для файлов
- **Валидация** типов и размеров файлов
- **Предварительный просмотр** выбранного файла
- **Автоматическая загрузка** на DigitalOcean Spaces

### 2. **Обновленная форма SampleCreateForm**
- Интегрирован компонент VideoUpload
- Поддержка загрузки thumbnail изображений
- Улучшенный UX с валидацией

### 3. **API Endpoint** (`src/app/api/samples/route.ts`)
- Поддержка **FormData** для загрузки файлов
- Поддержка **JSON** для обычных данных
- Автоматическое определение типа запроса
- Загрузка thumbnail на S3

### 4. **Обновленная API функция** (`src/lib/api.ts`)
- Умная отправка данных (FormData или JSON)
- Поддержка файлов в интерфейсе SampleData

## 🚀 Как использовать:

### **Создание нового Sample:**

1. **Перейдите в админ панель**: `/admin/samples`
2. **Нажмите "Create New Sample"**
3. **Заполните основную информацию**:
   - Название sample
   - Тип сервиса (опционально)

4. **Выберите видео**:
   - **Вариант 1**: Перетащите видео файл или выберите через кнопку
   - **Вариант 2**: Введите URL видео в поле

5. **Загрузите thumbnail** (опционально):
   - Выберите изображение для превью
   - Обрежьте по нужному соотношению сторон

6. **Нажмите "Create Sample"**

## 📁 Поддерживаемые форматы:

### **Видео файлы:**
- MP4, WebM, OGG, AVI, MOV, QuickTime
- **Максимальный размер**: 100MB
- **Автоматическая загрузка** на DigitalOcean Spaces

### **Thumbnail изображения:**
- JPEG, JPG, PNG, WebP
- **Максимальный размер**: 5MB
- **Соотношение сторон**: 16:9 (landscape)

## 🌐 URL структура:

### **Загруженные видео:**
```
https://arcast-s3.blr1.cdn.digitaloceanspaces.com/videos/samples/filename.mp4
```

### **Thumbnail изображения:**
```
https://arcast-s3.blr1.cdn.digitaloceanspaces.com/images/samples/filename.jpg
```

## 🔧 Технические детали:

### **Компонент VideoUpload:**
```tsx
<VideoUpload
  onVideoSelect={(videoUrl, videoFile) => {
    // videoUrl - URL видео (загруженного или введенного)
    // videoFile - объект File (если загружен файл)
  }}
  initialVideoUrl="https://example.com/video.mp4" // опционально
  className="w-full"
/>
```

### **API вызов:**
```typescript
const sampleData = {
  name: "Sample Name",
  videoUrl: "https://example.com/video.mp4", // или URL загруженного файла
  serviceTypeId: "service-type-id",
  thumbnailFile: file // опционально
}

const sample = await createSample(sampleData)
```

## 🎯 Преимущества:

1. **Гибкость**: выбор между загрузкой файла или URL
2. **Производительность**: CDN для быстрой загрузки
3. **Безопасность**: валидация типов и размеров файлов
4. **UX**: drag & drop, предварительный просмотр
5. **Масштабируемость**: автоматическое масштабирование через S3

## 🧪 Тестирование:

1. **Запустите приложение**: `npm run dev`
2. **Перейдите в админ панель**: `http://localhost:3000/admin/samples`
3. **Создайте новый sample** с видео
4. **Проверьте загрузку** файлов на DigitalOcean Spaces

## 🆘 Устранение проблем:

### **Ошибка загрузки видео:**
- Проверьте размер файла (максимум 100MB)
- Убедитесь в правильности формата
- Проверьте соединение с DigitalOcean Spaces

### **Ошибка загрузки thumbnail:**
- Проверьте размер изображения (максимум 5MB)
- Убедитесь в правильности формата
- Проверьте права доступа

### **Проблемы с URL:**
- Убедитесь, что URL ведет на прямой файл
- Проверьте доступность файла по ссылке

---

## 🎉 Готово к использованию!

Функциональность загрузки видео для samples полностью интегрирована и готова к использованию!
