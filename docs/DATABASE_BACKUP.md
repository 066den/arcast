# Database Backup & Restore Guide

Этот документ описывает различные способы создания и восстановления бекапов базы данных для проекта Arcast.

## 🚀 Быстрый старт

### Создание бекапа

```bash
npm run db:backup
```

### Восстановление из бекапа

```bash
npm run db:restore backups/backup-2024-01-15T10-30-00-000Z.json
```

## 📋 Доступные методы

### 1. Prisma Backup (Рекомендуемый)

Использует Prisma ORM для создания JSON бекапа всех данных.

**Преимущества:**

- ✅ Работает с любым типом базы данных
- ✅ Сохраняет все связи между таблицами
- ✅ Легко читаемый JSON формат
- ✅ Включает метаданные (timestamp, количество записей)

**Использование:**

```bash
# Создать бекап
npm run db:backup

# Восстановить из бекапа
npm run db:restore backups/backup-YYYY-MM-DDTHH-mm-ss-sssZ.json
```

### 2. PostgreSQL pg_dump (Если установлен PostgreSQL)

Создает SQL дамп базы данных.

**Установка PostgreSQL на Windows:**

1. Скачайте PostgreSQL с официального сайта
2. Установите с настройками по умолчанию
3. Добавьте `C:\Program Files\PostgreSQL\16\bin` в PATH

**Использование:**

```bash
# Создать SQL дамп
pg_dump -h localhost -U postgres -d arcast > backup.sql

# Восстановить из дампа
psql -h localhost -U postgres -d arcast < backup.sql
```

### 3. Docker Backup (Если используется Docker)

Создает бекап через Docker контейнер.

**Использование:**

```bash
# Создать бекап
docker exec -t arcast-postgres-1 pg_dump -U postgres arcast > backup.sql

# Восстановить из бекапа
docker exec -i arcast-postgres-1 psql -U postgres arcast < backup.sql
```

## 📁 Структура бекапов

### Prisma Backup

```
backups/
├── backup-2024-01-15T10-30-00-000Z.json
├── backup-2024-01-16T14-20-00-000Z.json
└── ...
```

### SQL Backup

```
backups/
├── backup-2024-01-15.sql
├── backup-2024-01-16.sql
└── ...
```

## 🔧 Автоматизация

### Ежедневные бекапы

Создайте bat файл для Windows:

```batch
@echo off
cd /d "D:\arcast"
npm run db:backup
echo Backup completed at %date% %time%
```

### Cron job для Linux/Mac:

```bash
# Добавить в crontab (crontab -e)
0 2 * * * cd /path/to/arcast && npm run db:backup
```

## ⚠️ Важные замечания

1. **Перед восстановлением** всегда создавайте текущий бекап
2. **Тестируйте восстановление** на тестовой базе данных
3. **Проверяйте размер** бекапов - большие файлы могут замедлить процесс
4. **Храните бекапы** в безопасном месте (не в репозитории)

## 🆘 Устранение проблем

### Ошибка подключения к базе данных

```bash
# Проверьте статус базы данных
npm run prisma:studio
```

### Ошибка при восстановлении

```bash
# Очистите базу данных перед восстановлением
npx prisma db push --accept-data-loss
```

### Большой размер бекапа

```bash
# Создайте бекап только определенных таблиц
# Отредактируйте scripts/backup-db.js
```

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи в консоли
2. Убедитесь, что база данных доступна
3. Проверьте права доступа к файлам
4. Обратитесь к документации Prisma
